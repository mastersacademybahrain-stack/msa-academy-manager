import { db, auth } from 'hatchable';
export const access='public'; export const methods=['POST'];
export default async function(req,res){const user=await auth.getUser(req);if(!user)return res.status(401).json({error:'Login required'});const {sport,day_of_week,start_time,coach_id,player_ids=[]}=req.body||{};if(!sport||day_of_week===undefined||!start_time)return res.status(400).json({error:'Missing session fields'});const x=await db.query('INSERT INTO academy_sessions(sport,day_of_week,start_time,coach_id) VALUES($1,$2,$3,$4) RETURNING id',[sport,day_of_week,start_time,coach_id||null]);const id=x.rows[0].id;for(const pid of player_ids){await db.query('INSERT INTO session_players(session_id,player_id) VALUES($1,$2)',[id,pid]);}res.json({id});}